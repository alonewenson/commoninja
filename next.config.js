/** @type {import('next').NextConfig} */
const nextConfig = {    reactStrictMode: true,
  images: {
      domains: ["website-assets.commoninja.com"],
      formats: ["image/webp"],
  },}

module.exports = nextConfig
